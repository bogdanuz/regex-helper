/**
 * REGEXHELPER v4.0
 * Main Entry Point
 */

import { initHeader } from './header.js';

// Initialize when DOM is ready
document.addEventListener('DOMContentLoaded', () => {
    console.log('RegexHelper v4.0 - Initializing...');
    
    // Initialize Header
    initHeader();
    
    console.log('RegexHelper v4.0 - Ready!');
});
